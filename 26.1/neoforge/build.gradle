plugins {
    id 'com.iamkaf.multiloader.neoforge'
}

dependencies {
    // Amber integration
    implementation libs.amber.neoforge
    implementation "com.iamkaf.konfig:konfig-neoforge:${konfig_version}"
}
